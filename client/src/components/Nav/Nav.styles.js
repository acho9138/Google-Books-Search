import { makeStyles } from '@material-ui/core/styles';

export const styles = makeStyles((theme) => ({
  root: {
    flexGrow: 1,
  },
  links: {
    color: "white",
  },
  title: {
    flexGrow: 1,
  },
}));